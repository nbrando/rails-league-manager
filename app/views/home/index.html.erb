<h1>Sports League App</h1>

<% if logged_in? %>
  <p>Welcome, <%= current_user.first_name %></p>
  <%= button_to "Logout", logout_path, method: :delete %>
<% else %>
  <%= link_to "Login", login_path %>
<% end %>
